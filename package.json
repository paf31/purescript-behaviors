{
  "name": "purescript-behaviors",
  "version": "0.1.0",
  "description": "A simple implementation of FRP which separates events and behaviors",
  "main": "",
  "scripts": {
    "make": "pulp build && pulp docs",
    "test": "npm run clean && mkdir -p output && webpack",
    "clean": "rm -rf output"
  },
  "private": true,
  "author": "Phil Freeman",
  "license": "MIT",
  "devDependencies": {
    "purs-loader": "^0.4.0",
    "uglify-js": "^2.6.1",
    "webpack": "^1.12.9"
  }
}
